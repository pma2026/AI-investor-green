[pytest]
# Backend modules import each other as top-level packages (e.g. `import trading`,
# `from agent.loop import ...`), so put backend/ on sys.path for the test run.
pythonpath = backend
testpaths = tests
addopts = -q
